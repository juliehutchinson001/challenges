class Node {
  constructor(data) {
    this.data = data;
    this.next = next;
  }
}

class Stack {
  constructor() {
    this.items = [];
  }

  isEmpty() {
    return this.items.length === 0;
  }

  insert(data) {
    this.items.push(data);
  }

  remove() {
    this.items.pop();
  }

  peek(data) {
    this.items.includes(data);
  }
}

const isValid = text => {};
